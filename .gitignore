# Nix build result symlinks
result
result-*

# Go build artifacts
# Compiled service binaries (local dev builds)
/golang/apps/service1/service1
/golang/apps/service2/service2
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# macOS
.DS_Store

# Editors
.idea/
.vscode/
*.swp

# NOTE: vendor/ is intentionally tracked — the flake builds with
# `src = ./.` and `vendorHash = null`, so Go deps must come from the repo.
